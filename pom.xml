<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      Maven coordinates identify this artifact (like a NuGet package id + version).
      Spring Boot's parent POM centralizes dependency versions (similar to Directory.Build.props
      or a shared targets file in .NET that pins Microsoft.* package versions).
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.1</version>
        <relativePath/>
    </parent>

    <groupId>com.tsdb</groupId>
    <artifactId>memtsdb</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>MemTSDB</name>
    <description>In-memory time-series database (interview-grade systems design)</description>

    <properties>
        <!--
          Java 21: virtual threads, sequenced collections, pattern matching refinements.
          In .NET terms, this is like targeting net8.0 with LangVersion=preview — the toolchain
          feature level for the compiler and runtime.
        -->
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <!--
          spring-boot-starter-web brings Spring MVC (ASP.NET Core MVC analogue), embedded Tomcat,
          Jackson JSON (System.Text.Json / Newtonsoft analogue), and validation.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!--
          springdoc-openapi: Swagger UI + OpenAPI 3 JSON at /v3/api-docs.
          Similar to Swashbuckle for ASP.NET Core.
        -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.7.0</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
              spring-boot-maven-plugin produces an executable "fat JAR" (self-contained deployment
              unit), comparable to dotnet publish producing a single-file or framework-dependent bundle.
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
